{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schema.openswitchboard.ai/v0/intent-card.json",
  "title": "Intent card",
  "description": "The only two objects that matter on the switchboard: a want ('looking_for') or a have ('offering'). Each one is a thin projection of intent - it deliberately excludes names, photos, addresses and free-form life detail. There are NO identity fields anywhere in a want or a have ('additionalProperties: false' plus the forbidden attribute-key list make this structural rather than advisory). Sensitive personal attributes (health, sexuality, beliefs, ...) are forbidden in both; they live client-side only. 'intent-card' is the wire name of this schema; to a person it is a want or a have.",
  "type": "object",
  "properties": {
    "schema_version": {
      "$ref": "common.json#/$defs/schemaVersion"
    },
    "type": {
      "description": "The side this takes. 'looking_for' is a want: my human is looking for this. 'offering' is a have: my human has this.",
      "type": "string",
      "enum": [
        "looking_for",
        "offering"
      ]
    },
    "category": {
      "$ref": "common.json#/$defs/category"
    },
    "geo": {
      "$ref": "common.json#/$defs/geoBucket"
    },
    "price": {
      "$ref": "common.json#/$defs/priceBand",
      "description": "MATCHING INPUT ONLY. On a want: the budget ceiling. On a have: the reserve floor. Never disclosed to a counterparty at any point; only deliberate terms (the 'ask' on a have, or an offer message) are disclosable."
    },
    "ask": {
      "$ref": "common.json#/$defs/ask",
      "description": "Haves only: a deliberate, disclosable asking price. Structurally forbidden on a want."
    },
    "attributes": {
      "$ref": "common.json#/$defs/attributes"
    },
    "urgency": {
      "description": "Routing hint only.",
      "type": "string",
      "enum": [
        "none",
        "days",
        "today"
      ],
      "default": "none"
    },
    "visibility": {
      "description": "v1 has exactly one visibility mode: a want or a have is anonymous until an introduction is made and disclosure is escalated by consent.",
      "type": "string",
      "enum": [
        "anonymous-until-introduced"
      ],
      "default": "anonymous-until-introduced"
    },
    "status": {
      "description": "'active' is matched normally. 'latent' is 'back pocket' intent: held by the switchboard and only surfaced when a real introduction appears.",
      "type": "string",
      "enum": [
        "active",
        "latent"
      ],
      "default": "active"
    },
    "ttl_days": {
      "description": "Days until it expires (INTENT_EXPIRED thereafter).",
      "type": "integer",
      "minimum": 1,
      "maximum": 90,
      "default": 60
    }
  },
  "required": [
    "schema_version",
    "type",
    "category",
    "geo"
  ],
  "additionalProperties": false,
  "allOf": [
    {
      "if": {
        "properties": {
          "type": {
            "const": "looking_for"
          }
        },
        "required": [
          "type"
        ]
      },
      "then": {
        "properties": {
          "ask": false
        }
      }
    }
  ]
}
